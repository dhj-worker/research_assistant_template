employ a shallow PointNet [57] to aggregate local geomet-            voxel training. Inspired by techniques used in real-time ren-

ric features within each voxel. Specifically, for each voxel         dering for efficient visibility culling [1], this approach acti-

vi  V, we apply a local max-pooling operation [55] to the            vates only the voxels within the camera's viewing frustum

points contained within that voxel, producing a feature vec-         during each training iteration. "Activating" a voxel means in-

tor fi. These voxel features F = {fi}, along with the sparse         cluding it in the isosurface extraction and rendering process.

structure V, are then fed into a sparse transformer backbone.        This "sectional" approach means we only process a portion

This backbone utilizes shifted window attention [39, 82],            of the 3D space at a time, markedly reducing memory usage.

similar to TRELLIS [78], but is adapted to operate directly          As illustrated in Fig. 3, given the camera's extrinsic , in-

on the sparse voxel features F and structure V. The trans-           trinsics K, and the near (n) and far (f ) clipping planes of the

former outputs a latent code z  Rdz , which represents the           viewing frustum, we compute the Model-View-Projection

encoded 3D shape.                                                    (MVP) matrix MVP. We then use a boolean operator

Decoder.  The decoder takes the latent code z as input               to check whether the center of each voxel vi lies within

                                                                     the viewing frustum defined by the MVP matrix. We use

and predicts the parameters of a SparseFlex instance, S =            I(vi  Frustum(MVP)) to represent this check, where I(·)

(V, Fc, Fv). We use a series of transformer layers, culmi-           is an indicator function. The set of active voxels, Vactive, is

nating in a final linear layer, to predict the SDF values (sj)       then defined as:

and deformations (j) for each corner grid, as well as the

interpolation weights (i, i) for each voxel.                         Vactive = {vi|I(vi  Frustum(MVP)) = 1, vi  V}.                    (2)

Upsampling Modules.      To achieve high-resolution recon-           Adaptive Frustum and Interior Reconstruction.           We in-

structions, we incorporate two convolutional, self-pruning           troduce a visibility ratio  (0 <   1) controlling the

upsampling modules within the decoder, following the trans-          proportion of active voxels in SparseFlex. We adaptively

former. These modules progressively increase the resolution          adjust the near and far clipping planes to ensure that approx-

of the SparseFlex representation. Each upsampling module             imately Nv voxels are within the frustum. This is achieved

subdivides existing voxels into smaller voxels (increasing           through an iterative process: we initially set the near and far

the resolution by a factor of 4). Crucially, each module also        planes and iteratively adjust them based on the number of

prunes redundant voxels based on a predicted occupancy               active voxels until the desired proportion is reached.

value. A voxel is considered occupied if, after the subdivi-         This adaptive frustum also enables a novel capability: re-

sion, it contains any points from the input point cloud P.           constructing mesh interiors using only rendering supervision.

This pruning process, inspired by [59], is essential for main-       By positioning a virtual camera inside the object or adjusting

taining the sparsity of the SparseFlex representation and            the near clipping plane to intersect the mesh, we can render

is particularly beneficial for accurately representing open          and supervise the internal structure (see Fig. 4). Moreover,

surfaces, as it removes unnecessary voxels in empty regions.         the zoom-in camera viewpoint can render the mesh surface

                                                                     with greater details, making it better for higher-resolution

3.3. Training SparseFlex VAE                                         training. This is a significant advantage over methods that

                                                                     rely on watertight representations, which cannot capture in-

We train our SparseFlex VAE end-to-end using rendering               terior details.

losses, leveraging the differentiability of the SparseFlex rep-      Loss Function.    We train our VAE in an end-to-end manner,

resentation and a new training strategy called frustum-aware         with an objective function comprising four components:

sectional voxel training. This strategy dramatically reduces

memory consumption during training, enabling us to achieve           L = 1Lrender + 2Lprune + 3LKL + 4Lflex,                           (3)

high resolutions (up to 10243) that would be infeasible with         where 1, 2, 3, and 4 are weighting coefficients that

traditional approaches.                                              balance the different loss terms.

Frustum-aware Sectional Voxel Training.       Even with the          Lrender is the rendering supervision loss. We employ a

sparse structure of SparseFlex, directly rendering the entire        combination of losses commonly used in differentiable ren-

representation at high resolutions can be computationally            dering [28, 49]:

expensive. Furthermore, standard rendering supervision typi-         Lrender = dLd + nLn + mLm + ssLss + lpLlp,                        (4)

cally focuses only on the visible surface, neglecting the inte-

rior of the shape. Besides, recent methods [36, 73, 75, 78, 80]      where Ld, Ln, and Lm denote the L1 loss for depth maps,

relying on rendering supervision from mesh typically require         normal maps, and mask maps, respectively. Lss and Llp

extracting the entire mesh because a dense representation            denote SSIM loss and LPIPS loss, and are only applied on

doesn't trivially allow for partial extraction. In contrast, our     normal maps. Please refer to the supplementary material

sparse representation naturally enables partial extraction. To       for a detailed definition of these losses and their weighting

address these issues, we introduce frustum-aware sectional           coefficients.

                                                                  5
                                                                        Method                       Toys4k                                Dora Benchmark

Lprune is the structure loss, formulated as a binary cross-                             CD           F1(0.001)    F1(0.01)     CD          F1(0.001)  F1(0.01) 

entropy (BCE) loss, supervising the construction of sparse              Craftsman [34]  13.08/4.63   10.13/15.15  56.51/85.02  13.54/2.06  6.30/11.14   73.71/91.95

                                                                        Dora [5]        11.15/2.13   17.29/26.55  81.54/93.84  16.61/1.08  13.65/25.78  78.73/96.40

voxels:                                                                 Trellis [78]    12.90/11.89  4.05/4.93    59.65/64.05  17.42/9.83  3.81/6.20    62.70/71.95

                Lprune = BCE     V, V^  ,                       (5)     XCube [59]      4.35/3.14    1.61/13.49   74.65/79.62  4.74/2.37   1.31/0.84    75.64/86.50

                                                                        3PSDF [6]       4.51/3.69    11.33/14.10  81.70/86.13  7.45/1.68   7.52/12.50   79.43/91.17

where V represents the ground-truth occupancy of voxels                 Ours256         2.56/1.25    18.31/27.23  85.35/92.01  1.93/0.53   16.24/28.37  88.76/97.31

derived from the input point cloud and V^ represents the                Ours512         1.67/0.84    23.74/34.10  90.39/95.60  1.36/0.23   21.85/36.03  91.55/98.51

                                                                        Ours1024        1.33/0.60    25.95/35.69  92.30/96.22  0.86/0.12   25.71/39.50  94.71/99.14

predicted occupancy by the upsampling modules.                          Table 1. Quantitative comparison for VAE reconstruction quality

LKL is the KL divergence between the learned latent dis-                on the Toys4K dataset (left) and Dora benchmark (right). The `/'

tribution and a standard normal prior, regularizing the latent          symbol separates the results computed over the entire dataset from

space. Lflex is the regularization term from Flexicubes [61]            those obtained exclusively on the watertight subset.

to encourage smooth SDF values.                                                         Method       CD           F1(0.001)    F1(0.01) 

3.4. Image-to-3D Generation with Rectified Flow                                         Surf-D [84]  63.79        0.80              23.17

                                                                                        3PSDF [6]    0.26         8.14              99.35

Building upon the trained SparseFlex VAE, we develop a                                  Ours256      0.55         6.35              94.88

pipeline for high-quality, image-conditioned 3D shape gen-                              Ours256      0.08         18.60             99.99

                                                                                        Ours512      0.18         11.31             99.93

eration, following a similar approach to TRELLIS [61]. Our                              Ours512      0.05         31.60             100.00

approach consists of two main components: Structure Flow                                Ours1024     0.05         24.80             100.00

                                                                                        Ours1024     0.04         37.22             100.00

Model and Structured Latent Flow Model.                                 Table 2. Reconstruction results on open-surface dataset Deep-

Structure Flow Model.  First, a separate, simple fully 3D               fashion3D.  indicates the absence of the self-pruning upsampling

convolutional structure VAE is employed to compress dense               module.

voxels representing 3D shapes into a low-resolution (1/4                of both VAE reconstruction and image-to-3D generation,

scale) space. Subsequently, the image condition features are            we apply a mesh preprocessing step to correct these flipped

extracted using DINOv2 [54] and injected into the trans-                normals, ensuring that all normals are consistently oriented

former model via cross-attention, after which a rectified flow          outwards. Please refer to the supplementary for more details.

model is trained within this low-resolution space. During               Building on the success of progressive training in recent

inference, given an input image, the trained structure flow             works [91], we train SparseFlex VAE progressively, increas-

model generates the corresponding low-resolution 3D space,              ing final resolution from low to high (256, 512, and 1024).

which is then decoded by the structure VAE to produce the               For the structure VAE and structure flow model, we adopt

sparse structure of the generated 3D shape.                             the model from Trellis [78] and finetune their pre-trained

Structured Latent Flow Model.    Based on the proposed                  weights to our task. We train SparseFlex VAE on 64 A100

SparseFlex VAE, the point cloud and the corresponding vox-              GPUs with a batch size of 64 and train structured latent flow

elized sparse structure of a 3D shape are encoded into a                models with a batch size of 256. We use the AdamW [44] op-

structured latent space. Subsequently, the image condition              timizer with an initial learning rate of 1e - 4 and the weight

feature obtained via DINOv2 is injected into the sparse trans-          decay as 0.01. At inference, we generate the results with 3.5

former model through cross-attention, followed by training              CFG and 50 sampling steps.

a rectified-flow model within this structured latent space.             4.2. Dataset, Baselines, and Metrics

During inference, given an input image, the corresponding

sparse structure is first generated using the structure flow            Dataset.        We evaluate the reconstruction quality of VAE

model and its structure VAE. Then, both the sparse structure            across different methods on a diverse set of datasets, includ-

and the input image are provided to the structured latent flow          ing 1) universal datasets ABO [12], GSO [15], Meta [48],

model to generate the corresponding structured latent repre-            Objaverse [14], Toys4k [64] and 2) open-surface dataset

sentation. Finally, the SparseFlex VAE decodes this latent              Deepfashion3D [19]). The test list of ABO, GSO, Meta, and

representation to produce the final 3D shape.                           Objaverse is derived from Dora benchmark [5] after exclud-

4. Experiments                                                          ing our training data which includes about 2.7k assets. For

                                                                        Toys4k, we use the full set following Trellis [78]. For the

4.1. Implementation Details                                             image-to-3D generation, we evaluate the methods on 200

                                                                        random assets from Toys4k [64] and some images in the

We develop the implementation of SparseFlex based on the                wild, showcasing the superior potential on generation tasks

official code1 provided by FlexiCubes [61]. We train Sparse-            of SparseFlex VAE.

Flex VAE and structured latent flow model using approx-                 Baselines.      We compare our VAE with previous state-of-

imately 400K high-quality 3D meshes filtered from large-                the-art methods, including Craftsman [34], Trellis [78],

scale datasets, Objaverse (-XL) [13, 14]. Since incorrect nor-

mals in raw data can significantly degrade the performance              1https://github.com/nv-tlabs/FlexiCubes

                                                                     6
Surface Error

GT

Ours

TRELLIS

XCube

Dora

CraftsMan

Figure 4. Qualitative comparison of VAE reconstruction between ours and other state-of-the-art baselines. Our approach demonstrate

superior performance in reconstructing complex shapes, open surfaces, and even interior structures.

Dora [5], XCubes [59], Surf-D [84] and 3PSDF [6], with              tive comparisons. We report the Fre´chet Inception Distance

Surf-D and 3PSDF specially designed for open surfaces. We           (FID) [20] and Kernel Inception Distance (KID) [2].

directly use the available pre-trained weights provided by

these baselines, except for 3PSDF, which we re-implement            4.3. VAE Reconstruction Evaluation

and train on our dataset. We only compare Surf-D on the             We conduct extensive experiments to evaluate the quantita-

Deepfashion3D dataset due to the lack of available pre-             tive results of VAE reconstruction from different methods in

trained weights trained on large-scale datasets. For the gener-     Table Tab. 1. Since Dora [5] and Craftsman [34] are trained

ation results, we compare our method with InstantMesh [80],         on watertight data, we separate the results exclusively on

Direct3D [77], and TRELLIS [78].                                    the watertight subset from the results computed over the

                                                                    entire dataset for the clear demonstration. These methods of-

Metrics.   We evaluate the reconstruction performance of            ten perform poorly on non-watertight meshes, especially

VAE by using the commonly used metrics, including Cham-             open surfaces like flowers. Ours256 already outperforms

fer Distance (CD) and F-score with thresholds of 0.01               other baselines in terms of CD and F-score. As the reso-

and 0.001. The metrics are multiplied by 104 and 102, re-           lution increases, our method achieves even better, ultimately

spectively. For generation results, four orthogonal views           achieving a 82% reduction on CD and 88% increase in

of normal maps for each shape are rendered for quantita-            F-score. Fig. 4 demonstrate the superiority of our method

                                                                 7
Surface Error

GT             TRELLIS-256       Ours-256       Ours-512         Ours-1024

Figure 5. Qualitative comparison of VAE reconstruction quality

between our method with different resolution and TRELLIS.

                   Feed-Forward Time (ms)  GPU Memory Cost (MB)

Resolution         2563  5123    10243     2563   5123           10243

Ours ( = 0.1)      333   620     1151      35515  40183          55441

Ours ( = 0.3)      357   697     1475      37403  45675          69991

w/o FSV            390   958     OOM       40703  62029          OOM

w/o FSV & Sp.      418   OOM     OOM       45505  OOM            OOM

Table 3. Feed-Forward time and GPU memory cost comparisons. 

stands for the visibility ratio of voxels. `OOM' means Out Of Memory

and `FSV' means frustum-aware sectional voxel training strategy. `Sp'

means SparseFlex.

Method         InstantMesh [80]  Direct3D [77]    TRELLIS [78]   Ours

FID                      68.74   50.84                    47.66  44.95

KID (×103)               9.68    2.04                     1.28   1.05

Table 4. Quantitative generation results on Toys4k.

for complex shapes, open surfaces, and interior structures.                    Figure 6. Single image-to-3D generations with in-the-wild im-

                                                                               ages.1 The geometry of generated assets accurately preserves sharp

Tab. 2 compares our method with those designed for open                        edges and fine details.

surfaces on the Deepfashion3D [19] dataset, and our method

still achieves the best performance among them.                                aware sectional voxel training strategy, we evaluate the run-

                                                                               time of the feed-forward and GPU memory consumption

4.4. Image to 3D Genration                                                     with different settings, as shown in Tab. 3. It demonstrates

We also validate the effectiveness of our SparseFlex VAE as                    that SparseFlex effectively reduces the required GPU mem-

a foundation model for generation. Tab. 4 validates the ef-                    ory and runtime during network feed-forward. However, it is

fectiveness of our generation. Visualizations are also demon-                  not efficient to scale to higher resolution. The frustum-aware

strated in Fig. 6, which includes image-to-3D results using                    sectional voxel training strategy eliminates the reliance on

wild images. The generated shapes, which preserves sharp                       the entire surface extraction during rendering, significantly

edges and fine details, highly match the given images and                      reducing the memory requirements during training.

showcase the generalization of our method.

                                                                               Sparse Voxel Resolutions.  Higher resolution leads to bet-

4.5. Ablation Studies                                                          ter VAE reconstruction quality as shown in Tab. 1. Fig. 5

Self-Pruning Upsampling.         Tab. 2 demonstrates that the                  illustrates the qualitative comparison of SparseFlex VAE

self-pruning upsampling module plays an important role                         with different resolutions, along with TRELLIS with a reso-

in the reconstruction quality of open-surface shapes, as it                    lution of 256. Thanks to geometry encoding, our approach

allows for effective pruning of voxels near open boundaries,                   achieves better geometry reconstruction with the same reso-

enabling the reconstruction boundary to better align with                      lution as TRELLIS. Benefiting from efficient training, more

input. Supplementary shows the visual effects of this module                   details of complex structures are revealed, such as the tank

in detail.                                                                     track in the first row, as the resolution increases.

SparseFlex and Frustum-aware Sectional Voxel Training.                         1The original images in Fig. 6 are sourced from various 3D generation

To demonstrate the effectiveness of SparseFlex and frustum-                    platforms, benchmarks (such as Tripo3D, Rodin, Meshy, Trellis, etc.).

                                                                            8
