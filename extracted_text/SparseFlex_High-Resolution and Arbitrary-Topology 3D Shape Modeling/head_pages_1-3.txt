                                           SparseFlex: High-Resolution and Arbitrary-Topology 3D Shape Modeling

                                        Xianglong He1,2 Zi-Xin Zou2 Chia-Hao Chen1,2 Yuan-Chen Guo2 Ding Liang2
                                                       Chun Yuan1 Wanli Ouyang3 Yan-Pei Cao2 Yangguang Li2

                                                    1Tsinghua University 2VAST 3The Chinese University of Hong Kong

arXiv:2503.21732v1 [cs.CV] 27 Mar 2025  Figure 1. SparseFlex VAE achieves high-fidelity reconstruction and generalization from point clouds. Benefiting from a sparse-
                                        structured differentiable isosurface surface representation and an efficient frustum-aware sectional voxel training strategy, our SparseFlex
                                        VAE demonstrates the state-of-the-art performance on complex geometries (left), open surfaces (top right), and even interior structures
                                        (bottom right), facilitating the high-quality image-to-3D generation with arbitrary topology.

                                                                Abstract                                     ing rendering, dramatically reducing memory consumption
                                                                                                             and enabling high-resolution training. This also allows, for
                                        Creating high-fidelity 3D meshes with arbitrary topology,            the first time, the reconstruction of mesh interiors using only
                                        including open surfaces and complex interiors, remains a             rendering supervision. Building upon this, we demonstrate
                                        significant challenge. Existing implicit field methods often         a complete shape modeling pipeline by training a varia-
                                        require costly and detail-degrading watertight conversion,           tional autoencoder (VAE) and a rectified flow transformer
                                        while other approaches struggle with high resolutions. This          for high-quality 3D shape generation. Our experiments show
                                        paper introduces SparseFlex, a novel sparse-structured iso-          state-of-the-art reconstruction accuracy, with a 82% reduc-
                                        surface representation that enables differentiable mesh re-          tion in Chamfer Distance and a 88% increase in F-score
                                        construction at resolutions up to 10243 directly from ren-           compared to previous methods, and demonstrate the gener-
                                        dering losses. SparseFlex combines the accuracy of Flexi-            ation of high-resolution, detailed 3D shapes with arbitrary
                                        cubes with a sparse voxel structure, focusing computation            topology. By enabling high-resolution, differentiable mesh
                                        on surface-adjacent regions and efficiently handling open            reconstruction and generation with rendering losses, Sparse-
                                        surfaces. Crucially, we introduce a frustum-aware sectional          Flex significantly advances the state-of-the-art in 3D shape
                                        voxel training strategy that activates only relevant voxels dur-     representation and modeling. Please see our project page at
                                                                                                             https://xianglonghe.github.io/TripoSF.
                                         Equal contribution.
                                         Corresponding authors.

                                                                                                          1
1. Introduction                                                      vates only the subset of SparseFlex voxels that reside within
                                                                     the camera's viewing frustum during each training iteration.
3D Generative AI is rapidly advancing, with applications             We also introduce an adaptive strategy to control the frus-
spanning entertainment, design, and robotics. However, gen-          tum's parameters, further optimizing memory usage. This
erating 3D content is fundamentally more challenging than            not only substantially reduces computational and memory
2D image or text generation due to the inherent complexity           overhead but also enables, for the first time, the reconstruc-
of representing and manipulating 3D geometry. Achieving              tion of mesh interiors using only rendering supervision by
high fidelity and supporting arbitrary topology--including           appropriately positioning the camera. Fig. 3 illustrates our
open surfaces and complex interiors--presents particularly           SparseFlex representation and the efficient sectional voxel
significant hurdles.                                                 training strategy.

   Recent progress in 3D generative models has explored var-            Building on SparseFlex and our frustum-aware training,
ious representations, including point clouds [45, 46, 52, 68],       we present a complete 3D shape modeling pipeline. We em-
meshes [51, 63], 3DGS [18, 32, 78, 88], and implicit                 ploy a variational autoencoder (VAE) architecture, drawing
fields [9, 10, 34, 35, 59, 77, 78, 87, 91, 93, 95]. Implicit         inspiration from TRELLIS [78] but with key modifications.
field representations, such as Signed Distance Functions             Firstly, because our focus is on high-fidelity geometry, we
(SDFs) and occupancy fields, have shown favorable re-                use point clouds as input to the VAE, providing a direct and
sults [31, 77, 78, 85, 87, 95]. However, creating training           detailed representation of the shape's surface. Furthermore,
data for these methods typically involves a two-step process         we introduce a self-pruning upsampling module within the
that limits their effectiveness. First, raw 3D mesh data must        decoder to further refine the sparse voxel structure, which
be converted into watertight representations [79, 91] to calcu-      is particularly beneficial for representing open surfaces. A
late SDF or occupancy values--a computationally expensive            rectified flow transformer is then trained on the learned la-
preprocessing step that often degrades fine details. Second,         tent space for high-quality, image-conditioned 3D shape
isosurfacing techniques (e.g., Marching Cubes [43], Dual             generation. Through extensive experiments on Toy4k [64],
Contouring [26]) are used to extract meshes from the learned         ABO [12], GSO [15], Meta [48], Objaverse [14], and Deep-
continuous field, which can introduce further inaccuracies           fashion3D [19], our method demonstrates state-of-the-art
and artifacts. While Unsigned Distance Fields (UDFs) offer a         shape reconstruction accuracy with minimal detail degrada-
potential way to model open surfaces [6, 11, 16, 38, 42, 84],        tion, and high-quality single-image 3D shape generation.
they often suffer from instability and struggle to capture
geometric fine details.                                                 Our main contributions are:

   Rendering-based supervision offers a powerful, differ-            · We propose SparseFlex, a new sparse-structured isosur-
entiable alternative for training 3D representations and 3D            face representation enabling efficient, high-resolution, and
generative models [60, 61, 73, 74, 78, 80]. By directly com-           differentiable 3D shape modeling, with natural handling
paring rendered images of a generated mesh to ground-truth             of open surfaces.
data, rendering losses avoid the need for the initial watertight
preprocessing step and better preserve fine details. However,        · We introduce a novel sectional voxel training strategy
a critical bottleneck arises: when used with dense implicit            with adaptive view frustum control, dramatically reducing
fields, rendering supervision requires extremely high mem-             memory consumption and enabling high-resolution mesh
ory consumption at high resolutions, severely limiting the             reconstruction and generation, including interiors, using
achievable fidelity.                                                   rendering losses.

   This paper introduces SparseFlex, a new sparse-                   · We demonstrate state-of-the-art reconstruction accuracy
structured isosurface representation that addresses these lim-         and the generation of high-resolution, detailed 3D shapes
itations and unlocks high-resolution, differentiable mesh              with arbitrary topology, representing a significant advance
reconstruction and generation using rendering supervisions.            in the field.
SparseFlex is built upon Flexicubes [61], providing accurate
and differentiable isosurface extraction. The key design is          2. Related Work
the use of a sparse voxel structure instead of a conventional
dense grid. This sparsity is crucial for two primary reasons:        2.1. 3D Shape Representations for Generation
(1) it dramatically reduces memory consumption, enabling
high-resolution modeling, and (2) it allows for the effec-           Point Cloud. Point clouds are a flexible 3D representation
tive pruning of voxels near open boundaries, enabling the            and can be easily acquired using depth sensors and LiDAR.
representation of open surfaces.                                     Research has focused on point cloud processing [57, 58, 92]
                                                                     and generation [45, 46, 52, 68], with recent approaches treat-
   To fully leverage the capabilities of SparseFlex, we pro-         ing point clouds as a distribution, directly sampling point
pose frustum-aware sectional voxel training. Inspired by             clouds from noise via generative models [4, 89]. However,
techniques used in real-time rendering [1], this approach acti-      due to their limitation in representing solid surfaces, an addi-
                                                                     tional surface reconstruction step [22, 27, 56] is required.

                                                                  2
Figure 2. Overview of the SparseFlex VAE pipeline. SparseFlex VAE takes point clouds sampled from a mesh as input, voxelizes them,
and aggregates their features into each voxel. A sparse transformer encoder-decoder compresses the structured feature into a more compact
latent space, followed by a self-pruning upsampling for higher resolution. Finally, the structured features are decoded to SparseFlex through
a linear layer. Using the frustum-aware section voxel training strategy, we can train the entire pipeline more efficiently by rendering loss.

Triangle Mesh. Triangle meshes are the primary represen-           on the surface. Although G-Shell [40] proposes a new 3D
tation for 3D assets in modern industrial pipelines. Recent        representation for extracting non-watertight meshes from
works employ auto-regressive models to generate triangle           watertight triangular meshes and trains a diffusion model
faces sequentially [51, 63, 66], improving resemblance to          based on this representation, its dense grid structure limits
artist-created meshes. While effective for low-poly meshes,        it to high resolution when handling complex shapes. In this
these methods struggle to produce high-quality meshes with         paper, we incorporate sparse structure into Flexicubes [61]
a high face count.                                                 to efficient high-quality isosurface representation for open
                                                                   faces.
Implicit Field. Implicit fields (SDF or occupancy) are
widely used in geometry learning, particularly for 3D re-          2.2. 3D Generative Models and VAE
construction [16, 22, 55, 61, 72, 96] and generation [9,
10, 34, 35, 59, 77, 78, 87, 91, 93, 95], producing high-           Existing 3D generation studies are primarily classified
quality meshes. Enhancements with triplane [77], vector            into two categories: large 3D reconstruction model follow-
set [85, 87, 93] sparse voxels [78] and sparse hierarchical        ing 2D multiview diffusion models and native 3D gener-
voxels [59] allow neural networks to decode the field more         ation models. The first category uses multiview diffusion
effectively. These methods typically use the isosurface tech-      models to generate multiview images from text or an im-
niques [26, 43, 53] for surface extraction but struggle with       age [23, 24, 37, 41, 62, 71], followed by a large 3D recon-
open-surface shapes like clothing and flowers. Additionally,       struction model [21, 33, 65, 67, 80, 81, 90, 97] is employed
many require a time-consuming watertight conversion that           to reconstruct the 3D representation from these images in
degrades details, while rendering supervised approaches [78]       seconds. However, inconsistencies between generated mul-
suffer from high memory consumption when training at high          tiview images significantly often degrade the result quality.
resolutions.                                                       The second category focuses on native 3D generation model
                                                                   that directly generate 3D models through generative mod-
Open Surfaces. Meshes with open surfaces are common                els, including GAN [3, 7, 76, 94], auto-regressive [50, 86],
but remain challenging to process. The Unsigned Distance           diffusion [25, 68, 87, 91] and rectified flow [35, 78].
Field (UDF) is widely used for open-surface modeling from
point clouds [8, 11, 70, 83] or multiview images [38, 42, 47].        Due to the diverse and non-compact nature of 3D repre-
Surf-D [84] introduces a UDF-based diffusion model for             sentation, many approaches use Variational Auto-Encoder
generating meshes with arbitrary topology. However, UDF-           (VAE) [29] or Vector Quantized VAE (VQ-VAE) [69] to en-
based shape modeling is more difficult than SDF, and its sur-      code 3D shapes into latent spaces for the generative models.
face extraction [17] is prone to instability due to inaccuracy     Geometry-focused methods often input point clouds uni-
in neural network gradient estimations. As a result, achiev-       formly sampled from mesh surfaces into the VAE [5, 87].
ing high-quality open-surface meshes remains challenging.          When aim at decoding both geometry and texture, some
3PSDF [6] introduces a three-pole sign to distinguish surface-     methods [31, 32, 78] encode multiview image features into
adjacent regions but relies on binary occupancy grids for          latent spaces. This two-stage process makes the VAE's re-
surface extraction, leading to discontinuities and artifacts       construction quality crucial for subsequent generation per-
                                                                   formance. Some works [5, 85] have improved generation
                                                                   quality by enhancing the VAE's shape encoding-decoding

                                                                3
