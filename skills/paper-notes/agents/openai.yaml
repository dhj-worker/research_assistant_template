interface:
  display_name: "Paper Notes"
  short_description: "Summarize local paper PDFs and continue Q&A notes."
  default_prompt: "Use $paper-notes to run 새 논문 정리 in this workspace."

policy:
  allow_implicit_invocation: true
